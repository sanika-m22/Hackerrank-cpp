#include <iostream>
using namespace std;

int main() {
    string a, b;
    cin >> a >> b;

    // Print lengths
    cout << a.size() << " " << b.size() << endl;

    // Print concatenation
    cout << a + b << endl;

    // Swap first characters
    char temp = a[0];
    a[0] = b[0];
    b[0] = temp;

    cout << a << " " << b << endl;

    return 0;
}
